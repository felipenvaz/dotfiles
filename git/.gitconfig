[alias]
	co = checkout
  slog = log --oneline
	amend = commit --amend --no-edit
	delete-branches = !git branch | grep -v master | xargs git branch -D
	rename = git mv -f
	fp = !git fetch && git prune
[pull]
	rebase = true
[rebase]
	autoStash = true
